# from repo root
cat > requirements.txt <<'EOF'
fastapi==0.111.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.1
httpx==0.27.0
pydantic==2.7.4

# DB & ORM
SQLAlchemy==2.0.31

# Migrations
alembic==1.13.1

# Crypto (Fernet/MultiFernet)
cryptography==42.0.7

# Optional: Postgres driver (skip for SQLite)
psycopg[binary]==3.1.19
EOF

# sanity check: the last line should be exactly psycopg[...] and the file should END there
nl -ba requirements.txt | sed -n '1,60p'
